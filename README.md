# portfolio

Portifólio em construção, feito com Vue 3 + TypeScript + Vite.

## Rodar localmente

```bash
npm install
npm run dev
```

## build de produção

```bash
npm run build
npm run preview   # pra conferir o build localmente antes de subir
```
